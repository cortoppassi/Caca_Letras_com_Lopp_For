const chamar = function() {

    const frase = document.getElementById('frase').value;
    const letra = document.getElementById('letra').value;

    for (var i = 0; i < frase.length; i++) {
        // console.log(frase[i]);

        if(frase[i] == letra){
         console.log(letra)

        }
    }
}
//Treinando conceitos basicos, Loop For!